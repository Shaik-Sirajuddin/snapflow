import { Theme } from "../theme.slint";

export component Panel inherits Rectangle {
    background: Theme.bg-surface;
    border-radius: Theme.radius-lg; // Uses the large border radius variant
    border-color: Theme.border-subtle;
    border-width: 1px;
    clip: true;
}