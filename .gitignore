# Rust build output
/sap-rust/target/
/panel-rust/target/
/panel-rust-spike/panel-rust/target/
/rui-acp-client/target/
/rui-acpx-client/target/
/lib/skills-manager/target/
/.rui-thread-cache/

# Local test and editor artifacts
/.DS_Store
/.idea/
/.vscode/

# shotcut-rebrand is checked out separately per this worktree's own VNC/CI
# workflow (fresh from Shaik-Sirajuddin/shotcut, not this repo's history) --
# never track it here.
/shotcut-rebrand/
/snapshotd/.snapshotd.build.lock

# live_oauth_browser_e2e.sh's Playwright dependency -- installed on
# demand by that script, not vendored (package.json/package-lock.json
# are committed, node_modules itself is not).
/panel-rust/tests/node_modules/
acpx/target/
/snapshotd/snapshotd

# Stray relative-path test export artifacts (some *_realsaprust_test.go
# tests pass a relative outputPath to file.export; when run from the repo
# root these land here instead of a tmp dir).
/snapshotd/internal/daemon/*.mp4
/snapshotd/internal/mcpadapter/*.mp4
