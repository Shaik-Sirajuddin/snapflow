// Dev-only viewer wrapper -- NOT part of production ui/ sources.
// Gives the real ChatPanel a sane initial window size around it so
// slint-viewer (which has no --width/--height flag and no host to
// drive resize) doesn't render at its cramped natural minimum size.
// Uses preferred-width/preferred-height (initial-size hint only) rather
// than width/height (which Slint treats as a fixed, non-resizable
// window size) so the window stays freely resizable/maximizable via
// the window manager after launch.
// Imports the real app.slint unmodified, so --auto-reload still
// picks up edits to any production .slint file.
//
// Mock chat data below is set as plain property-override bindings
// (not --load-data JSON) -- --load-data only reflects properties
// declared directly on the loaded component, not ones inherited
// from ChatPanel, so JSON-driven mock data silently fails
// ("ignoring unknown property: threads" etc). Overriding via normal
// Slint inheritance (like width/height below) works because these
// bindings are compiled directly against DevRoot, same mechanism.
import { ChatPanel } from "__CHATPANEL_SLINT__";

export component DevRoot inherits ChatPanel {
    preferred-width: 1600px;
    preferred-height: 1000px;
    // Index 0 is idle — must not pick a "loading" thread or ChatArea
    // sets sending=true and disables the compose TextInput.
    selected-thread: 0;
    connection-status: "Connected";
    compact: false;
    sidebar-expanded: true;
    narrow: false;
    agent-badge: "codex . ask";
    has-older-messages: true;
    loading-older-messages: false;

    // Settings / permission profile mocks so the sheet has chips to click.
    default-profile: "default";
    permission-profile: "default";
    available-profiles: [
        { name: "default", agent-id: "codex", terminal-enabled: true, fs-enabled: true },
        { name: "read-only", agent-id: "codex", terminal-enabled: false, fs-enabled: false },
        { name: "full-access", agent-id: "claude", terminal-enabled: true, fs-enabled: true },
    ];

    // Compose bar row 2 -- mode + model selectors. Capability-gated on
    // non-empty lists; seed them here so the VNC/slint-viewer fixture
    // shows the same controls the host fills from ACP.
    mode-trigger-label: "ask";
    mode-dropdown-entries: [
        { id: "ask", label: "ask", value: "ask", is-header: false, is-current: true },
        { id: "code", label: "code", value: "code", is-header: false, is-current: false },
        { id: "plan", label: "plan", value: "plan", is-header: false, is-current: false },
        { id: "architect", label: "architect", value: "architect", is-header: false, is-current: false },
    ];
    config-dropdown-entries: [
        { id: "model-hdr", label: "model", value: "", is-header: true, is-current: false },
        { id: "model-sonnet", label: "claude-sonnet-4", value: "claude-sonnet-4", is-header: false, is-current: true },
        { id: "model-haiku", label: "claude-haiku-4", value: "claude-haiku-4", is-header: false, is-current: false },
        { id: "model-opus", label: "claude-opus-4", value: "claude-opus-4", is-header: false, is-current: false },
        { id: "model-gpt", label: "gpt-4.1", value: "gpt-4.1", is-header: false, is-current: false },
        { id: "model-o3", label: "o3", value: "o3", is-header: false, is-current: false },
    ];

    threads: [
        {
            name: "Refactor auth middleware",
            status: "idle",
            busy: false,
            open: true,
            background: false,
            description: "Split session validation into its own module",
            closed: false,
            provider: "codex",
            model: "gpt-4.1",
        },
        {
            name: "Fix flaky upload test",
            status: "loading",
            busy: true,
            open: true,
            background: false,
            description: "Reproducing the race in CI, adding a retry",
            closed: false,
            provider: "claude",
            model: "claude-sonnet-4",
        },
        {
            name: "Investigate memory leak",
            status: "error",
            busy: false,
            open: true,
            background: true,
            description: "Heap growth in the render worker after ~2h",
            closed: false,
            provider: "claude",
            model: "claude-haiku-4",
        },
        {
            name: "Docs: onboarding guide",
            status: "closed",
            busy: false,
            open: true,
            background: false,
            description: "Draft reviewed and merged",
            closed: true,
            provider: "",
            model: "",
        },
    ];
    messages: [
        {
            kind: "user",
            text: "The upload integration test fails about 1 in 10 runs on CI, can you find out why?",
            markdown-lines: [],
            status: "",
            expanded: false,
            index: 0,
            raw-input: "",
            raw-output: "",
            queued: false,
            can-edit: false,
            sending: false,
            first-use: false,
        },
        {
            kind: "thinking",
            text: "Likely a race between the upload finishing and the test asserting on the resulting file -- checking the test harness's wait condition first.",
            markdown-lines: [],
            status: "",
            expanded: false,
            index: 1,
            raw-input: "",
            raw-output: "",
            queued: false,
            can-edit: false,
            sending: false,
            first-use: false,
        },
        {
            kind: "tool_use",
            text: "grep -rn \"wait_for_upload\" tests/",
            status: "COMPLETED",
            expanded: false,
            index: 2,
            raw-input: "{\"pattern\":\"wait_for_upload\",\"path\":\"tests/\"}",
            raw-output: "tests/support/upload_helpers.rs:42:pub fn wait_for_upload(...)\ntests/e2e/upload_flow.rs:18:    wait_for_upload(&client, id).await;",
            queued: false,
            can-edit: false,
            sending: false,
            first-use: false,
        },
        {
            kind: "agent",
            text: "Found it: `wait_for_upload` polls the DB row but not the object-store write, so the test can see the row committed before the blob is actually flushed. I'll add a blob-existence check to the poll loop.",
            markdown-lines: [],
            status: "",
            expanded: false,
            index: 3,
            raw-input: "",
            raw-output: "",
            queued: false,
            can-edit: false,
            sending: false,
            first-use: false,
        },
        {
            kind: "tool_use",
            text: "apply_patch tests/support/upload_helpers.rs",
            markdown-lines: [],
            status: "COMPLETED",
            expanded: false,
            index: 4,
            raw-input: "",
            raw-output: "",
            queued: false,
            can-edit: false,
            sending: false,
            first-use: false,
        },
        {
            kind: "user",
            text: "Nice catch. Run the suite a few times to make sure it's stable now.",
            markdown-lines: [],
            status: "",
            expanded: false,
            index: 5,
            raw-input: "",
            raw-output: "",
            queued: false,
            can-edit: false,
            sending: false,
            first-use: false,
        },
        {
            kind: "tool_use",
            text: "cargo test upload_flow -- --test-threads=1 --repeat=20",
            markdown-lines: [],
            status: "RUNNING",
            expanded: false,
            index: 6,
            raw-input: "",
            raw-output: "",
            queued: false,
            can-edit: false,
            sending: false,
            first-use: false,
        },
        {
            kind: "agent",
            text: "Running **20** iterations now — will report back once it finishes.\n\n- cargo test\n- check *flaky* races\n- ship `upload_flow`",
            markdown-lines: [],
            status: "",
            expanded: false,
            index: 7,
            raw-input: "",
            raw-output: "",
            queued: false,
            can-edit: false,
            sending: false,
            first-use: false,
        },
        {
            kind: "agent",
            text: "### Markdown renderer checklist\n\nBold **works**, italic *works*, and `inline code` works.\n\n1. ordered item\n2. second item\n\n| Col A | Col B |\n| --- | --- |\n| left | right |\n\nSee also [Slint docs](https://docs.slint.dev).",
            markdown-lines: [],
            status: "",
            expanded: false,
            index: 12,
            raw-input: "",
            raw-output: "",
            queued: false,
            can-edit: false,
            sending: false,
            first-use: false,
        },
        {
            kind: "skill_use",
            text: "artifact-design",
            markdown-lines: [],
            status: "",
            expanded: false,
            index: 13,
            raw-input: "{\"skill\":\"artifact-design\"}",
            raw-output: "",
            queued: false,
            can-edit: false,
            sending: false,
            first-use: true,
        },
        {
            kind: "mcp_server_call",
            text: "mcp__claude-in-chrome__tabs_context_mcp",
            markdown-lines: [],
            status: "COMPLETED",
            expanded: true,
            index: 14,
            raw-input: "{}",
            raw-output: "{\"tabs\":[{\"id\":7,\"title\":\"GitHub PR #142\",\"active\":true}]}",
            queued: false,
            can-edit: false,
            sending: false,
            first-use: false,
        },
    ];
}
